import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:noxi/app/modules/grid/grid_page.dart';

void main() {
  testWidgets('GridPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GridPage(title: 'Grid')));
    //  final titleFinder = find.text('Grid');
    //  expect(titleFinder, findsOneWidget);
  });
}
