import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:noxi/app/modules/authentication/form/form_page.dart';

void main() {
  testWidgets('FormPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FormPage(title: 'Form')));
    //  final titleFinder = find.text('Form');
    //  expect(titleFinder, findsOneWidget);
  });
}
