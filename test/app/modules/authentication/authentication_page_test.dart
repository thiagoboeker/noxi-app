import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:noxi/app/modules/authentication/authentication_page.dart';

void main() {
  testWidgets('AuthenticationPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AuthenticationPage(title: 'Authentication')));
    //  final titleFinder = find.text('Authentication');
    //  expect(titleFinder, findsOneWidget);
  });
}
