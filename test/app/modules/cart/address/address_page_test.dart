import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:noxi/app/modules/cart/address/address_page.dart';

void main() {
  testWidgets('AddressPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddressPage(title: 'Address')));
    //  final titleFinder = find.text('Address');
    //  expect(titleFinder, findsOneWidget);
  });
}
