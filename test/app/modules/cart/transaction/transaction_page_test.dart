import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:noxi/app/modules/cart/transaction/transaction_page.dart';

void main() {
  testWidgets('TransactionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TransactionPage(title: 'Transaction')));
    //  final titleFinder = find.text('Transaction');
    //  expect(titleFinder, findsOneWidget);
  });
}
